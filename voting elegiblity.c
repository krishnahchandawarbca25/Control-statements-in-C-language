#include <stdio.h>
void main()
{
    int num;
    printf("enter your age");
    scanf("%d", &num);
    if (num<=18)
    {
     printf("you are not elegible to vote");
    }
    else;
    printf("you are elegible to vote");

}
